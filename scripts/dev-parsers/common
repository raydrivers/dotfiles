#!/usr/bin/awk -f
# Shared helpers for dev parsers

function make_absolute(path) {
    if (path ~ /^[A-Za-z]:/ || path ~ /^\//) return path
    return DIR "/" path
}

function emit(file, line, col, msg) {
    print make_absolute(file) ":" line ":" col ": " msg
}
